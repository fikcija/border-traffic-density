# One image, so training and serving dependencies are merged.
# aarch64 note: `tensorflow`, not `tensorflow-cpu` - the -cpu wheels are x86_64 only.
tensorflow>=2.16
keras-tuner>=1.4
# keras_tuner imports tensorboard internally even without a TensorBoard callback,
# and the aarch64 tensorflow wheel does not pull it in.
tensorboard>=2.16
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
pillow>=10.0
matplotlib>=3.7

# orchestration
prefect>=3.0
mlflow>=2.14,<3
httpx>=0.27

# serving
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
slowapi>=0.1.9
pyjwt>=2.8

# process supervision
supervisor>=4.2
